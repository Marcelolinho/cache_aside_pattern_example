version: '3.8'
services:
  redis:
    image: redis:latest
    container_name: cache_db
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --requirepass marcelo
    restart: always

volumes:
  redis_data: